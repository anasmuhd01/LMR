mutable , immutable-bool

expression [having operands and operator, a+ b a, b --> operands ]


identifier - class name should start with capital letter

bitwise operator -- used to compare binary numbers
membership operator --> to check a value is a part of a collection

 in , not in      
 
---------------------
identity operator: is not, memory address checking



input() --> string , so convert to int()
controlled loop --> for
un controlled --> while

-----------------------------------------------------
a=10
b=20
print("a is big")if a>b else print('b is big')

python feature ---------------------------------------




Jumping statements -->
break ---> stops and moves interpreter out of loop
continue --> stops current loop and by-pass into next loop

for - else
an else block only executes when it provided with a looping block when that loop 
completely executed


#----POSITIONAL ARGS-----

#----KEYWORD ARGS---------

#-----ARBITRARY ARGUMENTS--------------------------------------
#----- " * " is given to the parameter inside the function ----
#----- value will store in a tuple-----------------------------


#---- ARBITRARY KEYWORD ARGUMENT---------
#---- " ** " WILL BE USED AND STORED INSIDE A DICTIONARY---


#------------------------------------------------------------

#-----DEFAULT ARGUMENTS-------------------------------------



Types of function------

-->Recurcive function


-->Lambda function
A lambda function is a small anonymous function.
A lambda function can take any number of arguments, but can only have one expression.



data types ::
string --> str = ''' ''' 

    eg str = ''' apple is 
                red'''

-string is immutable
-string is indexed
    position based representative value

-string is orderd

# a[6]="o"
# TypeError: 'str' object does not support item assignment


--> STRING CLASS METHOD <--
capitalize()  casefold      isalfa
isdecimal   idgigit     islower
isspace     isupper     join 
lower      replace      split  
casefold    count       endswith
find        format      index
isalnum     strip       swapcase 
title upper

find()--> will return -1 if the item is not find
        !-1
index()--> will return error if the item is not their
        !ValueError: substring not found

--------------->> TUPLE <<--------------------------

Immutable, Orderd, Indexed
eg:
 if (1) its <clas (int)>  --> (1,) 
(1,2,3,"hello",True)

tuple class method --
1. count
2. index

--------------------------LIST--------------------
--> list comprehension
newlist = [expression for item in iterable if condition == True]


LIST CLASS METHOD
append()	Adds an element at the end of the list
clear()	Removes all the elements from the list
copy()	Returns a copy of the list
count()	Returns the number of elements with the specified value
extend()	Add the elements of a list (or any iterable), to the end of the current list
index()	Returns the index of the first element with the specified value
insert()	Adds an element at the specified position
pop()	Removes the element at the specified position
remove()	Removes the first item with the specified value
reverse()	Reverses the order of the list
sort()	Sorts the list


DICTIONARY
key value pair
eg:
{key:value}
{"name":"anas","place":"v"}

-mutable     -orderd(after py 3.7)    -named index
-concationation not possible
-packing and unpacking not possible


------------------------------------------------------
-------------------------SET--------------------------

{1,2,3,"apple"}

-unorderd
-mutable
-non-indexed
-duplicate values will be ignored

set class method
add()
remove()


-----------------------------------------------------------
--------------------SLICING--------------------------------