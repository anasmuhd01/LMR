programming Paradigm: style of programming/format of programming;
                      there is several style of programming

a fundamental style, approach, or methodology for programming that 
dictates how a developer structures, organizes,
 and implements code to solve problems


eg:
imerative 
declarative
functional 
procedural
event driven
object oriented

Object Oriented
---------------
the data given to object

class- user defined data type, collection of objects, 
  |
  |-->Object: 

abstraction--> data hiding
encapsulation--> protecting data
inheritance
polymorphism

real world entity/ instance of a class
collection of objects / blueprint of an object / user-defined datatype
ability of an instace/class to show properites of other instance/class based on relationship
wrapping up data and behaviours into a single unit
ability of an instance to show different behaviours based on different situations
showing relevent data while hiding irrelevant data


class ClassName: --> class
  members

ob=ClassName()  --> object creation/ instaciation

default      \ 
parmeterized /

--> Dunder methods/ Magic methods
built in method exists in every class, where its name starts and ends with
" __ "(double underscore)

1. __init__() :--> constructor
2. __str__()  :--> string representative
3. __repr__()
4. __del__() :--> destructor

---- INHERITANCE ------
-----------------------
super()
parent class members can be acessed by using super method super().member_name

types:->

Single Inheritance
Single inheritance enables a derived class to inherit properties from a single parent class, 
thus enabling code reusability and the addition of new features to existing code.

Multiple Inheritance
When a class can be derived from more than one base class this type of inheritance is called multiple inheritances. 
In multiple inheritances, all the features of the base classes are inherited into the derived class. 

Multiple Inheritance
When a class can be derived from more than one base class this type of inheritance is called multiple inheritances.
In multiple inheritances, all the features of the base classes are inherited into the derived class. 

Hierarchical Inheritance
When more than one derived class are created from a single base this type of inheritance is called hierarchical 
inheritance. In this program, we have a parent (base) class and two child (derived) classes.

Hybrid Inheritance
Hybrid inheritance is a combination of more than one type of inheritance. It uses a mix like single, multiple, 
or multilevel inheritance within the same program. Python's method resolution order (MRO) handles such cases.

----------------------------------------------encapsulation--------------------------------------------------------------------------

-wrapping up of data and behaviours into a single unit is called encapsu;ation